- hosts: localhost
  become: true
  tasks:
  - name: Install Required and option packages
    apt: 
        name: ['build-essential', 'tree', 'htop', 'git']
  roles:
  - src: webarchitect609.jet_brains_toolbox
    version: 1.0.1
